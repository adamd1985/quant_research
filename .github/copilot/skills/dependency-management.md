# Skill: Dependency Management

- Use `requirements.txt` or `environment.yml` as source of truth.
- Add dependencies only when necessary and document why.
- Avoid network installs unless asked.
- If the task also touches notebook prose, avoid numbered signposts or abstract scaffolding headings. Keep titles natural and easy to parse.
