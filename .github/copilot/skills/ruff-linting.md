# Skill: Ruff Linting

- Install Ruff with `pip install ruff` if needed.
- Prefer `ruff check .` to surface lint issues.
- Use `ruff check . --fix` only when explicitly requested.
- When adding to deps, update `requirements.txt` or `environment.yml`.
