# Skill: Installing Python Packages

- Ask before installing new packages.
- Prefer `pip install -r requirements.txt` when setup is needed.
- Document any version pinning or conflicts.
- If package changes require notebook prose updates, keep headings natural and avoid numbered signposts.
